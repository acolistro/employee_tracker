<h1>Edit employee</h1>

<%= form_for @employee do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.collection_select(:division_id, Division.all, :id, :name) %>
  <%= f.submit  %>
<% end %>
